<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="icon" type="image/svg+xml" href="/images/favicon3.svg">
    <title>WedInk</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <style>
   .video-section {
    position: relative;
    width: 100%;
    height: 100vh; 
    overflow: hidden;
}


.video-container {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    overflow: hidden;
  
    background-color: rgba(0, 0, 0, 0.6) !important;
}


.video-container video {
    position: absolute;
    top: 50%;
    left: 50%;
    min-width: 100%;
    min-height: 100%;
    width: auto;
    height: auto;
    transform: translate(-50%, -50%);
    object-fit: cover;
    z-index: -1;
}

/* Content Overlay */
.content {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    text-align: center;
    color: white;
    z-index: 1;
}

/* Content Style */
h1 {
    font-size: 5rem !important;
}

p {
    font-size: 1.2rem; 
}

/* Larger h1 and smaller p on mobile screens */
@media (max-width: 912px) {
    h1 {
        font-size: 4rem !important;
        margin-bottom: .6rem !important; 
    }
    p {
        font-size: 0.7rem  !important; 
    }
    .btn {
        font-size: 1rem;
        padding: 12px 20px ;
    }
}

@media (min-width: 320px) and (max-width: 941px) {
    h1 {
        font-size: 4.5rem ;
    }
    p {
        font-size: 0.8rem;
    }
    .btn {
        font-size: 1.2rem;
        padding: 15px 25px;
    }

    .navbar-toggler {
    margin-left: 10px !important; 
}

}

@media (min-width: 768px) and (max-width: 1024px) {
    .navbar-toggler {
    margin-left: -30px !important; 
}
.navbar-brand{
    margin-right: -30px !important;
}
}




/* Styling for button */
.btn-lg {
    font-size: 1.5rem;
    padding: 15px 30px;
    margin-left: 15px;
}

/* Round button style */
.roundess {
    border-radius: 90% !important;
    font-size: 1rem;
}

@keyframes fadeBounce {
        0% {
            opacity: 0;
            transform: translateY(-30px);
        }
        50% {
            opacity: 0.5;
            transform: translateY(10px);
        }
        100% {
            opacity: 1;
            transform: translateY(0);
        }
    }

    /* Apply Animation */
    .animated-text {
        color: skyblue !important;
        text-decoration: underline !important;
        font-size: 5rem !important;
        animation: fadeBounce 1.5s ease-in-out forwards;
    }

    /* Responsive Size */
    @media (max-width: 768px) {
        .animated-text {
            font-size: 3.5rem !important;
        }
    }


    .pop-button {
        transition: transform 0.2s ease, box-shadow 0.2s ease;
        animation: popAnimation 1.5s infinite ease-in-out;
    }

    /* Pop Effect on Hover */
    .pop-button:hover {
        transform: scale(1.15);
        box-shadow: 0px 8px 20px rgba(0, 0, 0, 0.3);
    }

    /* Pop Effect on Click */
    .pop-button:active {
        transform: scale(0.9);
        box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
    }

    /* Infinite Pop Animation */
    @keyframes popAnimation {
        0% { transform: scale(1); }
        50% { transform: scale(1.1); }
        100% { transform: scale(1); }
    }

    </style>
</head>
<body>
  
  
   

  
    <%- include("../includes/navbar.ejs") %>

    <section class="video-section">
<div class="video-container">
  
    <video autoplay loop muted playsinline>
        <source src="images/8551791-uhd_3840_2160_25fps.mp4" type="video/mp4">
        Your browser does not support the video tag.
    </video>
</div>
<div class="content">
    <h1 id="animatedText" class="animated-text">Welcome</h1>


    <p>Ultimate destination for creating custom celebration cards online! Whether it's a wedding, birthday, or any special occasion, we help you design unique and personalized cards to suit your event perfectly </p>

   
    <form method="get" action="/listings">
        <button class="btn btn-primary btn-lg roundess pop-button">Explore</button>

    </form>

    </div></section>
 

<!-- footer  -->

<div class="foor ">

    <%- include("../includes/footer.ejs") %>
</div>
</body>
 

<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>

<script>
    function animateTextInfinite() {
        const text = "Welcome";
        const animatedTextElement = document.getElementById("animatedText");

        let index = 0;
        animatedTextElement.innerHTML = ""; // Clear text before each animation

        function showLetter() {
            if (index < text.length) {
                animatedTextElement.innerHTML += text[index];
                index++;
                setTimeout(showLetter, 400); // Delay each letter by 400ms
            } else {
                setTimeout(() => {
                    animatedTextElement.innerHTML = ""; // Clear text
                    index = 0; // Reset index
                    showLetter(); // Restart animation
                }, 6000); // Wait 1 sec before restarting
            }
        }

        showLetter();
    }

    document.addEventListener("DOMContentLoaded", animateTextInfinite);
</script>
</body>
</html>
