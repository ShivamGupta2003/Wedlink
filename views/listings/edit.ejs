<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Listing</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
    <style>
        .card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.2) !important;
        }
        .form-control:focus {
            box-shadow: 0 0 0 0.25rem rgba(40, 167, 69, 0.25);
            border-color: #28a745;
            transition: all 0.3s ease;
        }
        .btn-success {
            transition: all 0.3s ease;
        }
        .btn-success:hover {
            transform: scale(1.05);
            box-shadow: 0 5px 15px rgba(40, 167, 69, 0.4);
        }
        .form-label {
            transition: color 0.3s ease;
        }
        .form-control:focus + .form-label,
        .form-control:not(:placeholder-shown) + .form-label {
            color: #28a745;
        }
    </style>
</head>
<body class="bg-light">
    <% layout("/layouts/boilerplate") %>
    <div class="container mt-5">
        <h2 class="text-center mb-4 animate__animated animate__fadeInDown">Edit Listing</h2>

        <div class="row justify-content-center">
            <div class="col-lg-6 col-md-8 col-sm-12">
                <div class="card shadow-lg p-4 animate__animated animate__fadeInUp">
                    <form method="POST" action="/listings/<%= listing._id %>?_method=PUT" class="needs-validation" novalidate enctype="multipart/form-data">
                        
                        <div class="mb-3 animate__animated animate__fadeInLeft" style="animation-delay: 0.2s">
                            <label class="form-label fw-bold">Shop Name</label>
                            <input name="listing[shopName]" value="<%= listing.shopName %>" class="form-control border-success" placeholder="Enter Shop Name" type="text" required>
                            <div class="invalid-feedback">Please enter the shop name.</div>
                        </div>

                        <div class="mb-3 animate__animated animate__fadeInRight" style="animation-delay: 0.3s">
                            <label class="form-label fw-bold">Card Description</label>
                            <textarea name="listing[description]" class="form-control border-success" placeholder="Enter Card Description" rows="3" required><%= listing.description %></textarea>
                            <div class="invalid-feedback">Please provide a description of the card.</div>
                        </div>

                        <div class="mb-3 animate__animated animate__fadeInLeft" style="animation-delay: 0.4s">
                            <label class="form-label fw-bold"> Upload New Image</label>
                            <input name="listing[image]"  class="form-control border-success"  type="file" >
                           
                        </div>

                       

                        <div class="mb-3 animate__animated animate__fadeInLeft" style="animation-delay: 0.6s">
                            <label class="form-label fw-bold">Country</label>
                            <input name="listing[country]" value="<%= listing.country %>" class="form-control border-success" placeholder="Enter Shop Country" type="text" required>
                            <div class="invalid-feedback">Please enter the country name.</div>
                        </div>

                        <div class="mb-3 animate__animated animate__fadeInRight" style="animation-delay: 0.7s">
                            <label class="form-label fw-bold">Location</label>
                            <input name="listing[location]" value="<%= listing.location %>" class="form-control border-success" placeholder="Enter Shop Location" type="text" required>
                            <div class="invalid-feedback">Please enter the shop location.</div>
                        </div>

                        <div class="mb-4 animate__animated animate__fadeInLeft" style="animation-delay: 0.8s">
                            <label class="form-label fw-bold">Phone Number</label>
                            <div class="input-group">
                                <span class="input-group-text bg-success text-white">📞</span>
                                <input name="listing[phoneNumber]" value="<%= listing.phoneNumber %>" class="form-control border-success" placeholder="Enter Shop Phone Number" type="number" required>
                            </div>
                            <div class="invalid-feedback">Please enter a valid phone number.</div>
                        </div>

                        <div class="text-center animate__animated animate__fadeInUp" style="animation-delay: 0.9s">
                            <button type="submit" class="btn btn-success btn-lg w-100 shadow">
                                <i class="bi bi-check-circle me-2"></i>Update Listing
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Add floating labels behavior
        document.querySelectorAll('.form-control').forEach(input => {
            input.addEventListener('focus', function() {
                this.closest('.mb-3').querySelector('.form-label').style.color = '#28a745';
            });
            input.addEventListener('blur', function() {
                if (!this.value) {
                    this.closest('.mb-3').querySelector('.form-label').style.color = '';
                }
            });
        });
    </script>
</body>
</html>