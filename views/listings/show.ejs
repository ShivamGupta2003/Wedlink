<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Marriage Listings</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* General Styling */
        body {
            background: #f8f9fa;
        }

        .listing-container {
            background: white;
            padding: 20px;
            border-radius: 15px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            border: 5px solid #d4af37;
            max-width: 900px;
            margin: auto;
        }

        h3 {
            color: #b88b2b;
            font-size: 24px;
            font-weight: bold;
            text-align: center;
        }

        .card {
            background: #fff;
            padding: 15px;
            border-radius: 10px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            text-align: left;
        }

        ul {
            list-style: none;
            padding: 0;
        }

        li {
            font-size: 16px;
            padding: 8px 0;
            color: #444;
        }

        .price {
            font-weight: bold;
            color: #d48806;
        }

        .no-listing {
            color: #ff4d4f;
            font-size: 18px;
            font-weight: bold;
            text-align: center;
        }

        /* Image Styling */
        .listing-img {
            width: 100%;
            border-radius: 10px;
            object-fit: cover;
            height: 100%;
            max-height: 350px;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .listing-container {
                width: 95%;
                padding: 15px;
            }

            h3 {
                font-size: 20px;
            }

            .listing-img {
                max-height: 250px;
            }
        }
    </style>
</head>
<body>
    <% layout("/layouts/boilerplate") %>

    <hr>
    <div class="container">
        <div class="listing-container">
            <h3>Marriage Card Listings</h3>

            <% if (listing) { %>  
                <div class="row align-items-center">
                    <!-- Image Section -->
                    <div class="col-md-6">
                        <img src="<%=listing.image %>" alt="Marriage Card Image" class="listing-img">
                    </div>

                    <!-- Content Section -->
                    <div class="col-md-6">
                        <div class="card">
                            <ul>
                                <li><strong>Shop Name:</strong> <%= listing.shopName %></li>
                                <li><strong>Description:</strong> <%= listing.description %></li> 
                                <li class="price">&#8377; <%= (listing.price ? listing.price.toLocaleString("en-IN") : "N/A") %></li> 
                                <li><strong>Location:</strong> <%= listing.location %></li> 
                                <li><strong>Country:</strong> <%= listing.country %></li>
                            </ul>
                        </div>
                    </div>
                </div>

                <div class="text-center mt-3">
                    <a href="/listings/<%= listing._id %>/edit" class="btn btn-warning">Edit Listing</a>
                    <form method="POST" action="/listings/<%= listing._id %>?_method=DELETE" class="d-inline">
                        <button class="btn btn-danger">Delete Card</button>
                    </form>
                </div>
            <% } else { %>
                <p class="no-listing">No listing found.</p>
            <% } %>
        </div>
    </div>
    <br>


    <hr>
    <div class="col-7 offset-3 mb-3">
        <h4>Leave a Review</h4>
        <form action="/listings/<%= listing.id %>/reviews" method="POST" novalidate class="needs-validation">
          <div class="mb-3 mt-3">
            <label for="rating" class="form-label">Rating</label>
            <input type="range" min="1" max="5" id="rating" name="review[rating]" class="form-range">
          </div>
          <div class="mb-3 mt-3">
            <label for="comment" class="form-label">Comment</label>
            <textarea name="review[comment]" id="comment" cols="30" rows="5" class="form-control" required></textarea>
            <div class="invalid-feedback">Please provide a valid comment.</div>
          </div>
          <br>
          <button class="btn btn-danger">Submit</button>
        </form>
      </div>

      
      <hr />

      <p class="fw-bold fs-5">All Reviews</p>
      
      <div class="row g-3">
        <% for (let review of listing.reviews) { %>
          <div class="col-lg-3 col-md-6 col-12">
            <div class="card shadow-sm h-100 border-0">
              <div class="card-body p-3">
                <p class="card-text"><%= review.comment %></p>
                <p class="card-text text-warning fw-bold">
                  <%= '★'.repeat(review.rating) %><%= '☆'.repeat(5 - review.rating) %>
                  <span class="text-dark fw-normal">(<%= review.rating %> stars)</span>
                </p>
              </div>
              <div class="card-footer bg-white border-0 text-center">
                <form method="POST" action="/listings/<%= listing._id %>/reviews/<%= review._id %>?_method=DELETE">
                  <button class="btn btn-sm btn-dark w-100 fw-bold">Delete</button>
                </form>
              </div>
            </div>
          </div>
        <% } %>
      </div>
      

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
</body>
</html>
